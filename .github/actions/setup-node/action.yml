runs:
  using: "composite"
  steps:
    - name: Setup Node
      uses: actions/setup-node@v6
      with:
        node-version: '20'
    - name: Install NPM packages
      shell: bash
      run: npm install
